services:
  postgres:
    image: postgres:latest
    container_name: article-summarizer-db
    environment:
      POSTGRES_USER: summarizer
      POSTGRES_PASSWORD: summarizer123
      POSTGRES_DB: article_summarizer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: